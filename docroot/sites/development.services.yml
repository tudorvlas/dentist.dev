# Local development services.
#
# Disable theme caching.
parameters:
  twig.config:
    debug : true
    auto_reload: true
    cache: false
services:
  cache.backend.null:
    class: Drupal\Core\Cache\NullBackendFactory
